• Полный VPC (public и private subnet);
• Поднять в AWS при помощи Terraform следующие ресурсы:
    ◦ два сервера (bastion host и приватный сервер);
    ◦ security groups;
    ◦ RDS‑инстанс.
• Для описания ресурсов использовать Terraform‑модули, а не всё в одном файле.
• Разобраться, как в модулях взаимодействуют между собой файлы variables.tf и output.tf:
    ◦ как объявляются входные переменные;
    ◦ как значения из модулей пробрасываются наружу через outputs.
• Настроить security group так, чтобы доступ на порт 22 был только с твоего IP‑адреса.
• Использовать переменную типа object для передачи необходимых IP для подключения на порт 22 (передать IP + имя правила).
